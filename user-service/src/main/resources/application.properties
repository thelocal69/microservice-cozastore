spring.application.name=user-service

server.port=${USER.PORT:8083}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://${MYSQL_HOST:localhost}:3306/cozastore_authenticate_service}
spring.datasource.username=root
spring.datasource.password=${MYSQL_ROOT_PASSWORD:618619}

spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false

eureka.client.service-url.defaulZone=http://localhost:${EUREKA.PORT:8761}/eureka

